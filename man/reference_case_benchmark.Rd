% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-reference-benchmark.R
\name{reference_case_benchmark}
\alias{reference_case_benchmark}
\title{Benchmark packaged reference cases}
\usage{
reference_case_benchmark(
  cases = c("synthetic_truth", "synthetic_bias_contract", "study1_itercal_pair",
    "study2_itercal_pair", "combined_itercal_pair"),
  method = "MML",
  model = "RSM",
  quad_points = 7,
  maxit = 40,
  reltol = 1e-06
)
}
\arguments{
\item{cases}{Reference cases to run. Defaults to all package-native
benchmark cases.}

\item{method}{Estimation method passed to \code{\link[=fit_mfrm]{fit_mfrm()}}. Defaults to \code{"MML"}.}

\item{model}{Model family passed to \code{\link[=fit_mfrm]{fit_mfrm()}}. Defaults to \code{"RSM"}.}

\item{quad_points}{Quadrature points for \code{method = "MML"}.}

\item{maxit}{Maximum optimizer iterations passed to \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{reltol}{Convergence tolerance passed to \code{\link[=fit_mfrm]{fit_mfrm()}}.}
}
\value{
An object of class \code{mfrm_reference_benchmark}.
}
\description{
Benchmark packaged reference cases
}
\details{
This function audits \code{mfrmr} against the package's curated internal
benchmark cases in three ways:
\itemize{
\item \code{synthetic_truth}: checks whether recovered facet measures align with the
known generating values from the package's internal synthetic design.
\item \code{synthetic_bias_contract}: checks whether package-native bias tables and
pairwise local comparisons satisfy the identities documented in the bias
help workflow.
\item \verb{*_itercal_pair}: compares a baseline packaged dataset with its iterative
recalibration counterpart to review fit stability, facet-measure
alignment, and linking coverage together.
}

The resulting object is intended as an internal benchmark harness for
package QA and regression auditing. It does not by itself establish
external validity against FACETS, ConQuest, or published calibration
studies, and it does not assume any familiarity with external table
numbering or printer layouts.
}
\section{Interpreting output}{

\itemize{
\item \code{overview}: one-row internal-benchmark summary.
\item \code{case_summary}: pass/warn/fail triage by reference case.
\item \code{fit_runs}: fitted-run metadata (fit, precision tier, convergence).
\item \code{design_checks}: exact design recovery checks for each dataset.
\item \code{recovery_checks}: known-truth recovery metrics for the internal synthetic
case.
\item \code{bias_checks}: source-backed bias/local-measure identity checks.
\item \code{pair_checks}: paired-dataset stability screens for the iterated cases.
\item \code{linking_checks}: common-element audits for paired calibration datasets.
\item \code{source_profile}: source-backed rules that define the internal benchmark
contract.
}
}

\examples{
\donttest{
bench <- reference_case_benchmark(
  cases = "synthetic_truth",
  method = "JML",
  maxit = 30
)
summary(bench)
}
}
