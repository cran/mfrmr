% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.R
\name{mfrm_misfit_thresholds}
\alias{mfrm_misfit_thresholds}
\title{MnSq misfit threshold pair used across mfrmr screening helpers}
\usage{
mfrm_misfit_thresholds(lower = NULL, upper = NULL)
}
\arguments{
\item{lower}{Optional lower bound. When \code{NULL} (default), the
active option / package default is used.}

\item{upper}{Optional upper bound.}
}
\value{
A named numeric vector \code{c(lower = ..., upper = ...)} with
\code{lower < upper}.
}
\description{
Returns the lower / upper bounds that mfrmr screens treat as the
acceptable mean-square (Infit / Outfit MnSq) band when flagging
element-level misfit. Defaults follow Linacre's published 0.5-1.5
acceptance band; both ends can be overridden via R options.
}
\details{
Helpers that consume the band include
\code{\link[=summary.mfrm_diagnostics]{summary.mfrm_diagnostics()}} (\code{misfit_flagged} block and
\code{key_warnings} auto-flag), \code{\link[=build_misfit_casebook]{build_misfit_casebook()}} (the new
\code{element_fit} source family), the bias / misfit narrative inside
\code{\link[=build_apa_outputs]{build_apa_outputs()}}, and \code{\link[=facet_quality_dashboard]{facet_quality_dashboard()}} when
\code{misfit_warn = NULL}. Setting the options once at the top of an
analysis script therefore changes every downstream screen at once.
}
\section{Configuration}{

Two scalar R options drive the band:
\describe{
\item{\code{mfrmr.misfit_lower}}{Lower acceptance bound. Default \code{0.5}.}
\item{\code{mfrmr.misfit_upper}}{Upper acceptance bound. Default \code{1.5}.}
}

Pass scalar arguments to override the options for a single call,
e.g. \code{mfrm_misfit_thresholds(lower = 0.7, upper = 1.3)} for the
tighter Bond & Fox (2015) reporting band.
}

\examples{
mfrm_misfit_thresholds()
old <- options(mfrmr.misfit_lower = 0.7, mfrmr.misfit_upper = 1.3)
mfrm_misfit_thresholds()
options(old)
}
\seealso{
\code{\link[=summary.mfrm_diagnostics]{summary.mfrm_diagnostics()}}, \code{\link[=build_misfit_casebook]{build_misfit_casebook()}},
\code{\link[=facet_quality_dashboard]{facet_quality_dashboard()}}
}
