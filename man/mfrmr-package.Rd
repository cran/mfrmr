% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfrmr-package.R
\docType{package}
\name{mfrmr-package}
\alias{mfrmr}
\alias{mfrmr-package}
\title{mfrmr: Many-Facet Rasch Modeling in R}
\description{
\code{mfrmr} provides estimation, diagnostics, and reporting utilities for
many-facet Rasch models (MFRM) using a native R implementation.
}
\details{
Recommended workflow:
\enumerate{
\item Fit model with \code{\link[=fit_mfrm]{fit_mfrm()}}
\item Compute diagnostics with \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}
\item Run residual PCA with \code{\link[=analyze_residual_pca]{analyze_residual_pca()}} if needed
\item Estimate interactions with \code{\link[=estimate_bias]{estimate_bias()}}
\item Build narrative/report outputs with \code{\link[=build_apa_outputs]{build_apa_outputs()}} and \code{\link[=build_visual_summaries]{build_visual_summaries()}}
}

Guide pages:
\itemize{
\item \link{mfrmr_workflow_methods}
\item \link{mfrmr_visual_diagnostics}
\item \link{mfrmr_reports_and_tables}
\item \link{mfrmr_reporting_and_apa}
\item \link{mfrmr_linking_and_dff}
\item \link{mfrmr_compatibility_layer}
}

Companion vignettes:
\itemize{
\item \code{vignette("mfrmr-workflow", package = "mfrmr")}
\item \code{vignette("mfrmr-visual-diagnostics", package = "mfrmr")}
\item \code{vignette("mfrmr-reporting-and-apa", package = "mfrmr")}
\item \code{vignette("mfrmr-linking-and-dff", package = "mfrmr")}
}

Function families:
\itemize{
\item Model fitting: \code{\link[=fit_mfrm]{fit_mfrm()}}, \code{\link[=summary.mfrm_fit]{summary.mfrm_fit()}}, \code{\link[=plot.mfrm_fit]{plot.mfrm_fit()}}
\item Legacy-compatible workflow wrapper: \code{\link[=run_mfrm_facets]{run_mfrm_facets()}}, \code{\link[=mfrmRFacets]{mfrmRFacets()}}
\item Diagnostics: \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}, \code{summary(diag)},
\code{\link[=analyze_residual_pca]{analyze_residual_pca()}}, \code{\link[=plot_residual_pca]{plot_residual_pca()}}
\item Bias and interaction: \code{\link[=estimate_bias]{estimate_bias()}}, \code{\link[=estimate_all_bias]{estimate_all_bias()}},
\code{summary(bias)}, \code{\link[=bias_interaction_report]{bias_interaction_report()}}, \code{\link[=plot_bias_interaction]{plot_bias_interaction()}}
\item Differential functioning: \code{\link[=analyze_dff]{analyze_dff()}}, \code{\link[=analyze_dif]{analyze_dif()}},
\code{\link[=dif_interaction_table]{dif_interaction_table()}}, \code{\link[=plot_dif_heatmap]{plot_dif_heatmap()}}, \code{\link[=dif_report]{dif_report()}}
\item Design simulation: \code{\link[=build_mfrm_sim_spec]{build_mfrm_sim_spec()}}, \code{\link[=extract_mfrm_sim_spec]{extract_mfrm_sim_spec()}},
\code{\link[=simulate_mfrm_data]{simulate_mfrm_data()}}, \code{\link[=evaluate_mfrm_design]{evaluate_mfrm_design()}},
\code{\link[=evaluate_mfrm_signal_detection]{evaluate_mfrm_signal_detection()}}, \code{\link[=predict_mfrm_population]{predict_mfrm_population()}},
\code{\link[=predict_mfrm_units]{predict_mfrm_units()}}, \code{\link[=sample_mfrm_plausible_values]{sample_mfrm_plausible_values()}} (including
fit-derived empirical / resampled / skeleton-based simulation
specifications; fixed-calibration unit scoring currently requires
\code{method = "MML"})
\item Reporting: \code{\link[=build_apa_outputs]{build_apa_outputs()}}, \code{\link[=build_visual_summaries]{build_visual_summaries()}},
\code{\link[=reporting_checklist]{reporting_checklist()}}, \code{\link[=apa_table]{apa_table()}}
\item Dashboards: \code{\link[=facet_quality_dashboard]{facet_quality_dashboard()}}, \code{\link[=plot_facet_quality_dashboard]{plot_facet_quality_dashboard()}}
\item Export / reproducibility: \code{\link[=build_mfrm_manifest]{build_mfrm_manifest()}}, \code{\link[=build_mfrm_replay_script]{build_mfrm_replay_script()}},
\code{\link[=export_mfrm_bundle]{export_mfrm_bundle()}}
\item Equivalence: \code{\link[=analyze_facet_equivalence]{analyze_facet_equivalence()}}, \code{\link[=plot_facet_equivalence]{plot_facet_equivalence()}}
\item Data and anchors: \code{\link[=describe_mfrm_data]{describe_mfrm_data()}}, \code{\link[=audit_mfrm_anchors]{audit_mfrm_anchors()}},
\code{\link[=make_anchor_table]{make_anchor_table()}}, \code{\link[=load_mfrmr_data]{load_mfrmr_data()}}
}

Data interface:
\itemize{
\item Input analysis data is long format (one row per observed rating).
\item Packaged simulation data is available via \code{\link[=load_mfrmr_data]{load_mfrmr_data()}} or \code{data()}.
}
}
\section{Interpreting output}{

Core object classes are:
\itemize{
\item \code{mfrm_fit}: fitted model parameters and metadata.
\item \code{mfrm_diagnostics}: fit/reliability/flag diagnostics.
\item \code{mfrm_bias}: interaction bias estimates.
\item \code{mfrm_dff} / \code{mfrm_dif}: differential-functioning contrasts and screening summaries.
\item \code{mfrm_population_prediction}: scenario-level forecast summaries for one
future design.
\item \code{mfrm_unit_prediction}: fixed-calibration posterior summaries for future
or partially observed persons.
\item \code{mfrm_plausible_values}: approximate fixed-calibration posterior draws for
future or partially observed persons.
\item \code{mfrm_bundle} families: summary/report bundles and plotting payloads.
}
}

\section{Typical workflow}{

\enumerate{
\item Prepare long-format data.
\item Fit with \code{\link[=fit_mfrm]{fit_mfrm()}}.
\item Diagnose with \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}.
\item Run \code{\link[=analyze_dff]{analyze_dff()}} or \code{\link[=estimate_bias]{estimate_bias()}} when fairness or interaction
questions matter.
\item Report with \code{\link[=build_apa_outputs]{build_apa_outputs()}} and \code{\link[=build_visual_summaries]{build_visual_summaries()}}.
\item For design planning, move to \code{\link[=build_mfrm_sim_spec]{build_mfrm_sim_spec()}},
\code{\link[=evaluate_mfrm_design]{evaluate_mfrm_design()}}, and \code{\link[=predict_mfrm_population]{predict_mfrm_population()}}.
\item For future-unit scoring, refit or retain an \code{MML} calibration and then
use \code{\link[=predict_mfrm_units]{predict_mfrm_units()}} or \code{\link[=sample_mfrm_plausible_values]{sample_mfrm_plausible_values()}}.
}
}

\section{Model formulation}{

The many-facet Rasch model (MFRM; Linacre, 1989) extends the basic Rasch
model by incorporating multiple measurement facets into a single linear
model on the log-odds scale.

\strong{General MFRM equation}

For an observation where person \eqn{n} with ability \eqn{\theta_n} is
rated by rater \eqn{j} with severity \eqn{\delta_j} on criterion \eqn{i}
with difficulty \eqn{\beta_i}, the probability of observing category
\eqn{k} (out of \eqn{K} ordered categories) is:

\deqn{P(X_{nij} = k \mid \theta_n, \delta_j, \beta_i, \tau) =
  \frac{\exp\bigl[\sum_{s=1}^{k}(\theta_n - \delta_j - \beta_i - \tau_s)\bigr]}
       {\sum_{c=0}^{K}\exp\bigl[\sum_{s=1}^{c}(\theta_n - \delta_j - \beta_i - \tau_s)\bigr]}}

where \eqn{\tau_s} are the Rasch-Andrich threshold (step) parameters and
\eqn{\sum_{s=1}^{0}(\cdot) \equiv 0} by convention.  Additional facets
enter as additive terms in the linear predictor
\eqn{\eta = \theta_n - \delta_j - \beta_i - \ldots}.

This formulation generalises to any number of facets; the
\code{facets} argument to \code{\link[=fit_mfrm]{fit_mfrm()}} accepts an arbitrary-length
character vector.

\strong{Rating Scale Model (RSM)}

Under the RSM (Andrich, 1978), all levels of the step facet share a
single set of threshold parameters \eqn{\tau_1, \ldots, \tau_K}.

\strong{Partial Credit Model (PCM)}

Under the PCM (Masters, 1982), each level of the designated \code{step_facet}
has its own threshold vector on the package's common observed score scale.
In the current implementation, threshold locations may vary by step-facet
level, but the fitted score range is still defined by one global category
set taken from the observed data.
}

\section{Estimation methods}{

\strong{Marginal Maximum Likelihood (MML)}

MML integrates over the person ability distribution using Gauss-Hermite
quadrature (Bock & Aitkin, 1981):

\deqn{L = \prod_{n} \int P(\mathbf{X}_n \mid \theta, \boldsymbol{\delta})
  \, \phi(\theta) \, d\theta
  \approx \prod_{n} \sum_{q=1}^{Q} w_q \,
  P(\mathbf{X}_n \mid \theta_q, \boldsymbol{\delta})}

where \eqn{\phi(\theta)} is the assumed normal prior and
\eqn{(\theta_q, w_q)} are quadrature nodes and weights.  Person
estimates are obtained post-hoc via Expected A Posteriori (EAP):

\deqn{\hat{\theta}_n^{\mathrm{EAP}} =
  \frac{\sum_q \theta_q \, w_q \, L(\mathbf{X}_n \mid \theta_q)}
       {\sum_q w_q \, L(\mathbf{X}_n \mid \theta_q)}}

MML avoids the incidental-parameter problem and is generally preferred
for smaller samples.

\strong{Joint Maximum Likelihood (JML/JMLE)}

JMLE estimates all person and facet parameters simultaneously as fixed
effects by maximising the joint log-likelihood
\eqn{\ell(\boldsymbol{\theta}, \boldsymbol{\delta} \mid \mathbf{X})}
directly.  It does not assume a parametric person distribution, which
can be advantageous when the population shape is strongly non-normal,
but parameter estimates are known to be biased when the number of
persons is small relative to the number of items (Neyman & Scott, 1948).

See \code{\link[=fit_mfrm]{fit_mfrm()}} for practical guidance on choosing between the two.
}

\section{Statistical background}{

Key statistics reported throughout the package:

\strong{Infit (Information-Weighted Mean Square)}

Weighted average of squared standardized residuals, where weights are the
model-based variance of each observation:

\deqn{\mathrm{Infit}_j = \frac{\sum_i Z_{ij}^2 \, \mathrm{Var}_i \, w_i}
                              {\sum_i \mathrm{Var}_i \, w_i}}

Expected value is 1.0 under model fit.  Values below 0.5 suggest overfit
(Mead-style responses); values above 1.5 suggest underfit (noise or
misfit).  Infit is most sensitive to unexpected patterns among on-target
observations (Wright & Masters, 1982).

Note: The 0.5--1.5 range is a widely used rule of thumb (Bond & Fox,
2015).  Acceptable ranges may differ by context: 0.6--1.4 for high-stakes
testing; 0.7--1.3 for clinical instruments; up to 0.5--1.7 for surveys
and exploratory work (Linacre, 2002).

\strong{Outfit (Unweighted Mean Square)}

Simple average of squared standardized residuals:

\deqn{\mathrm{Outfit}_j = \frac{\sum_i Z_{ij}^2 \, w_i}{\sum_i w_i}}

Same expected value and flagging thresholds as Infit, but more sensitive
to extreme off-target outliers (e.g., a high-ability person scoring the
lowest category).

\strong{ZSTD (Standardized Fit Statistic)}

Wilson-Hilferty cube-root transformation that converts the mean-square
chi-square ratio to an approximate standard normal deviate:

\deqn{\mathrm{ZSTD} = \frac{\mathrm{MnSq}^{1/3} - (1 - 2/(9\,\mathit{df}))}
                           {\sqrt{2/(9\,\mathit{df})}}}

Values near 0 indicate expected fit; \eqn{|\mathrm{ZSTD}| > 2} flags
potential misfit at the 5\\% level, and \eqn{|\mathrm{ZSTD}| > 3} at the
1\\% level (Wright & Linacre, 1994).  ZSTD is reported alongside every
Infit and Outfit value.

\strong{PTMEA (Point-Measure Correlation)}

Pearson correlation between observed scores and estimated person measures
within each facet level.  Positive values indicate that scoring aligns
with the latent trait dimension; negative values suggest reversed
orientation or scoring errors.

\strong{Separation}

Package-reported separation is the ratio of adjusted true standard deviation
to root-mean-square measurement error:

\deqn{G = \frac{\mathrm{SD}_{\mathrm{adj}}}{\mathrm{RMSE}}}

where \eqn{\mathrm{SD}_{\mathrm{adj}} =
\sqrt{\mathrm{ObservedVariance} - \mathrm{ErrorVariance}}}. Higher values
indicate the facet discriminates more statistically distinct levels along the
measured variable. In \code{mfrmr}, \code{Separation} is the model-based value and
\code{RealSeparation} provides a more conservative companion based on \code{RealSE}.

\strong{Reliability}

\deqn{R = \frac{G^2}{1 + G^2}}

Analogous to Cronbach's alpha or KR-20 for the reproducibility of element
ordering. In \code{mfrmr}, \code{Reliability} is the model-based value and
\code{RealReliability} gives the conservative companion based on \code{RealSE}. For
\code{MML}, these are anchored to observed-information \code{ModelSE}
estimates for non-person facets; \code{JML} keeps them as exploratory summaries.

\strong{Strata}

Number of statistically distinguishable groups of elements:

\deqn{H = \frac{4G + 1}{3}}

Three or more strata are commonly used as a practical target
(Wright & Masters, 1982), but in this package the estimate inherits the
same approximation limits as the separation index.
}

\section{Key references}{

\itemize{
\item Andrich, D. (1978). A rating formulation for ordered response
categories. \emph{Psychometrika}, 43, 561--573.
\item Bond, T. G., & Fox, C. M. (2015). \emph{Applying the Rasch model} (3rd
ed.). Routledge.
\item Linacre, J. M. (1989). \emph{Many-facet Rasch measurement}. MESA Press.
\item Linacre, J. M. (2002). What do Infit and Outfit, mean-square and
standardized mean? \emph{Rasch Measurement Transactions}, 16(2), 878.
\item Masters, G. N. (1982). A Rasch model for partial credit scoring.
\emph{Psychometrika}, 47, 149--174.
\item Wright, B. D., & Masters, G. N. (1982). \emph{Rating scale analysis}.
MESA Press.
\item Wright, B. D., & Linacre, J. M. (1994). Reasonable mean-square fit
values. \emph{Rasch Measurement Transactions}, 8(3), 370.
}
}

\section{Model selection}{

\strong{RSM vs PCM}

The Rating Scale Model (RSM; Andrich, 1978) assumes all levels of the
step facet share identical threshold parameters.  The Partial Credit
Model (PCM; Masters, 1982) allows each level of the \code{step_facet} to have
its own set of thresholds on the package's shared observed score scale.
Use RSM when the rating rubric is identical across all items/criteria;
use PCM when category boundaries are expected to vary by item or criterion.
In the current implementation, PCM still assumes one common observed score
support across the fitted data, so it should not be described as a fully
mixed-category model with arbitrary item-specific category counts.

\strong{MML vs JML}

Marginal Maximum Likelihood (MML) integrates over the person ability
distribution using Gauss-Hermite quadrature and does not directly estimate
person parameters; person estimates are computed post-hoc via Expected A
Posteriori (EAP).  Joint Maximum Likelihood (JML/JMLE) estimates all
person and facet parameters simultaneously as fixed effects.

MML is generally preferred for smaller samples because it avoids the
incidental-parameter problem of JML.  JML is faster and does not assume
a normal person distribution, which may be an advantage when the
population shape is strongly non-normal.

See \code{\link[=fit_mfrm]{fit_mfrm()}} for usage.
}

\examples{
mfrm_threshold_profiles()
list_mfrmr_data()

}
\seealso{
Useful links:
\itemize{
  \item \url{https://ryuya-dot-com.github.io/R_package_mfrmr/}
  \item \url{https://github.com/Ryuya-dot-com/R_package_mfrmr}
  \item Report bugs at \url{https://github.com/Ryuya-dot-com/R_package_mfrmr/issues}
}

}
\author{
\strong{Maintainer}: Ryuya Komuro \email{ryuya.komuro.c4@tohoku.ac.jp} (\href{https://orcid.org/0000-0001-9205-0926}{ORCID})

}
