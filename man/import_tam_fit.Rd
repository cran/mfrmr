% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-import.R
\name{import_tam_fit}
\alias{import_tam_fit}
\title{Import a \code{TAM} fit to an mfrmr-compatible bundle}
\usage{
import_tam_fit(
  fit,
  model = c("RSM", "PCM", "GPCM"),
  item_facet = "Item",
  compute_fit = FALSE
)
}
\arguments{
\item{fit}{An object returned by \code{TAM::tam.mml()},
\code{TAM::tam.jml()}, or \code{TAM::tam.mml.mfr()}.}

\item{model}{Same as \code{\link[=import_mirt_fit]{import_mirt_fit()}}.}

\item{item_facet}{Name to assign to the item facet for the
single-facet path. Ignored when the input is a multi-facet
\code{tam.mml.mfr} fit (the original facet names are preserved).}

\item{compute_fit}{Logical. When \code{TRUE}, run \code{\link[TAM:tam.fit]{TAM::tam.fit()}}
and \code{\link[TAM:tam.personfit]{TAM::tam.personfit()}} to populate Infit / Outfit columns
on the returned facet tables, plus build a measurement-side
\code{mfrm_diagnostics} bundle. Default \code{FALSE}.}
}
\value{
An \code{mfrm_imported_fit} object. Slots mirror
\code{\link[=import_mirt_fit]{import_mirt_fit()}}.
}
\description{
Extracts item / step / person parameters from a \code{\link[TAM:tam.mml]{TAM::tam.mml()}},
\code{\link[TAM:tam.jml]{TAM::tam.jml()}}, or \code{\link[TAM:tam.mml.mfr]{TAM::tam.mml.mfr()}} fit. The multi-facet
\code{tam.mml.mfr()} path is detected automatically and each
non-person facet is mapped onto a row of \code{fit$facets$others}
so downstream MFRM helpers (e.g. \code{plot_qc_dashboard()}) work
on the imported object.
}
\seealso{
\code{\link[=import_mirt_fit]{import_mirt_fit()}}, \code{\link[=import_erm_fit]{import_erm_fit()}}
}
