% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-simulation-future-branch.R
\name{plot.mfrm_future_branch_active_branch}
\alias{plot.mfrm_future_branch_active_branch}
\title{Plot a future arbitrary-facet planning active branch}
\usage{
\method{plot}{mfrm_future_branch_active_branch}(
  x,
  y = NULL,
  type = c("profile_metrics", "load_balance", "coverage", "readiness_tiers",
    "table_rows", "role_tables", "appendix_roles", "appendix_sections",
    "appendix_presets", "selection_handoff_presets", "selection_tables",
    "selection_handoff", "selection_handoff_bundles", "selection_handoff_roles",
    "selection_handoff_role_sections", "selection_bundles", "selection_roles",
    "selection_sections"),
  appendix_preset = c("recommended", "compact", "all", "methods", "results",
    "diagnostics", "reporting"),
  selection_value = c("count", "fraction"),
  draw = TRUE,
  main = NULL,
  palette = NULL,
  label_angle = 45,
  ...
)
}
\arguments{
\item{x}{Output from the future-branch active planning scaffold stored in
\code{planning_schema$future_branch_active_branch}.}

\item{y}{Unused placeholder for generic compatibility.}

\item{type}{Plot type: \code{"profile_metrics"} for recommended deterministic
profile values by metric, \code{"load_balance"} for recommended load/balance
values by metric, \code{"coverage"} for recommended coverage/connectivity values
by metric, \code{"readiness_tiers"} for counts of structural tiers across the
current active-branch design grid, \code{"table_rows"} / \code{"role_tables"} /
\code{"appendix_roles"} for summary-table bundle QC,
\code{"appendix_sections"} / \code{"appendix_presets"} for manuscript-facing
appendix selection counts, \code{"selection_handoff_presets"} for preset-level
appendix handoff counts, \code{"selection_tables"} for appendix-selected
future-branch tables ranked by row count within a preset,
\code{"selection_handoff"} for section-aware plot-ready appendix handoff counts,
\code{"selection_handoff_bundles"} for section-and-bundle plot-ready appendix
handoff counts,
\code{"selection_handoff_roles"} for role-aware plot-ready appendix handoff
counts, \code{"selection_handoff_role_sections"} for role-by-section plot-ready
appendix handoff counts,
or \code{"selection_bundles"} / \code{"selection_roles"} / \code{"selection_sections"}
for preset-filtered appendix selection summaries.}

\item{appendix_preset}{Appendix preset used for \verb{selection_*} plot types.}

\item{selection_value}{For \verb{selection_*} plot types, whether to plot exact
counts (\code{"count"}) or the matching exact fraction (\code{"fraction"}) when that
surface exposes one. \code{selection_tables} remains count-only because it
represents table row counts rather than a normalized selection surface.}

\item{draw}{If \code{TRUE}, draw with base graphics; otherwise return plotting data.}

\item{main}{Optional title override.}

\item{palette}{Optional named color overrides.}

\item{label_angle}{Axis-label rotation angle.}

\item{...}{Reserved for generic compatibility.}
}
\value{
A plotting-data object of class \code{mfrm_plot_data}.
}
\description{
Plot a future arbitrary-facet planning active branch
}
\seealso{
\code{\link[=summary.mfrm_future_branch_active_branch]{summary.mfrm_future_branch_active_branch()}}
}
