% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-reports.R
\name{as_kable.apa_table}
\alias{as_kable.apa_table}
\title{Convert an \code{apa_table} to a \code{knitr::kable()} object}
\usage{
\method{as_kable}{apa_table}(x, format = c("pipe", "html", "latex"), digits = 3L, ...)
}
\arguments{
\item{x}{An \code{apa_table} object from \code{\link[=apa_table]{apa_table()}}.}

\item{format}{One of \code{"pipe"} (default, Markdown), \code{"html"}, or
\code{"latex"}, passed through to \code{knitr::kable()}.}

\item{digits}{Numeric; passed to \code{knitr::kable()}.}

\item{...}{Additional arguments forwarded to \code{knitr::kable()}.}
}
\value{
A \code{knitr_kable} object ready to be printed inline in a
report, or a message when \code{knitr} is unavailable.
}
\description{
Renders the table data for direct inclusion in RMarkdown,
Quarto, or HTML reports, wiring the \code{caption} and \code{note} slots
into the standard APA placement (caption above, note below).
When \code{kableExtra} is installed the note is attached as a footer for HTML or
LaTeX. Otherwise the complete rendered table is collapsed to one
\code{knitr_kable} string before a single note is appended.
}
\examples{
tbl <- structure(
  list(
    table = data.frame(Term = c("Rater A", "Rater B"), Estimate = c(-0.12, 0.18)),
    caption = "Facet estimates",
    note = "Toy values for formatting only."
  ),
  class = "apa_table"
)
if (requireNamespace("knitr", quietly = TRUE)) {
  invisible(as_kable(tbl))
}
}
\seealso{
\code{\link[=as_flextable.apa_table]{as_flextable.apa_table()}}, \code{\link[=apa_table]{apa_table()}}.
}
