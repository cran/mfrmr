% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-simulation.R
\name{simulate_mfrm_data}
\alias{simulate_mfrm_data}
\title{Simulate long-format many-facet Rasch data for design studies}
\usage{
simulate_mfrm_data(
  n_person = 50,
  n_rater = 4,
  n_criterion = 4,
  raters_per_person = n_rater,
  score_levels = 4,
  theta_sd = 1,
  rater_sd = 0.35,
  criterion_sd = 0.25,
  noise_sd = 0,
  step_span = 1.4,
  group_levels = NULL,
  dif_effects = NULL,
  interaction_effects = NULL,
  seed = NULL,
  model = c("RSM", "PCM"),
  step_facet = "Criterion",
  thresholds = NULL,
  assignment = NULL,
  sim_spec = NULL
)
}
\arguments{
\item{n_person}{Number of persons/respondents.}

\item{n_rater}{Number of rater facet levels.}

\item{n_criterion}{Number of criterion/item facet levels.}

\item{raters_per_person}{Number of raters assigned to each person.}

\item{score_levels}{Number of ordered score categories.}

\item{theta_sd}{Standard deviation of simulated person measures.}

\item{rater_sd}{Standard deviation of simulated rater severities.}

\item{criterion_sd}{Standard deviation of simulated criterion difficulties.}

\item{noise_sd}{Optional observation-level noise added to the linear predictor.}

\item{step_span}{Spread of step thresholds on the logit scale.}

\item{group_levels}{Optional character vector of group labels. When supplied,
a balanced \code{Group} column is added to the simulated data.}

\item{dif_effects}{Optional data.frame describing true group-linked DIF
effects. Must include \code{Group}, at least one design column such as
\code{Criterion}, and numeric \code{Effect}.}

\item{interaction_effects}{Optional data.frame describing true non-group
interaction effects. Must include at least one design column such as
\code{Rater} or \code{Criterion}, plus numeric \code{Effect}.}

\item{seed}{Optional random seed.}

\item{model}{Measurement model recorded in the simulation setup.}

\item{step_facet}{Step facet used when \code{model = "PCM"} and threshold values
vary across levels. Currently \code{"Criterion"} and \code{"Rater"} are supported.}

\item{thresholds}{Optional threshold specification. Use either a numeric
vector of common thresholds or a data frame with columns \code{StepFacet},
\code{Step}/\code{StepIndex}, and \code{Estimate}.}

\item{assignment}{Assignment design. \code{"crossed"} means every person sees
every rater; \code{"rotating"} uses a balanced rotating subset; \code{"resampled"}
reuses person-level rater-assignment profiles stored in \code{sim_spec};
\code{"skeleton"} reuses an observed response skeleton stored in \code{sim_spec},
including optional \code{Group}/\code{Weight} columns when available. When omitted,
the function chooses \code{"crossed"} if
\code{raters_per_person == n_rater}, otherwise \code{"rotating"}.}

\item{sim_spec}{Optional output from \code{\link[=build_mfrm_sim_spec]{build_mfrm_sim_spec()}} or
\code{\link[=extract_mfrm_sim_spec]{extract_mfrm_sim_spec()}}. When supplied, it defines the generator setup;
direct scalar arguments are treated as legacy inputs and should generally
be left at their defaults except for \code{seed}.}
}
\value{
A long-format \code{data.frame} with core columns \code{Study}, \code{Person},
\code{Rater}, \code{Criterion}, and \code{Score}. If group labels are simulated or
reused from an observed response skeleton, a \code{Group} column is included.
If a weighted response skeleton is reused, a \code{Weight} column is also
included.
}
\description{
Simulate long-format many-facet Rasch data for design studies
}
\details{
This function generates synthetic MFRM data from the Rasch model.
The data-generating process is:
\enumerate{
\item Draw person abilities: \eqn{\theta_n \sim N(0, \texttt{theta\_sd}^2)}
\item Draw rater severities: \eqn{\delta_j \sim N(0, \texttt{rater\_sd}^2)}
\item Draw criterion difficulties: \eqn{\beta_i \sim N(0, \texttt{criterion\_sd}^2)}
\item Generate evenly-spaced step thresholds spanning \eqn{\pm}\code{step_span/2}
\item For each observation, compute the linear predictor
\eqn{\eta = \theta_n - \delta_j - \beta_i + \epsilon} where
\eqn{\epsilon \sim N(0, \texttt{noise\_sd}^2)} (optional)
\item Compute category probabilities under the recorded measurement model
(\code{RSM} or \code{PCM}) and sample the response
}

Latent-value generation is explicit:
\itemize{
\item \code{latent_distribution = "normal"} draws centered normal person/rater/
criterion values using the supplied standard deviations
\item \code{latent_distribution = "empirical"} resamples centered support values
recorded in \code{sim_spec$empirical_support}
}

When \code{dif_effects} is supplied, the specified logit shift is added to
\eqn{\eta} for the focal group on the target facet level, creating a
known DIF signal.  Similarly, \code{interaction_effects} injects a known
bias into specific facet-level combinations.

The generator targets the common two-facet rating design (persons
\eqn{\times} raters \eqn{\times} criteria).  \code{raters_per_person}
controls the incomplete-block structure: when less than \code{n_rater},
each person is assigned a rotating subset of raters to keep coverage
balanced and reproducible.

Threshold handling is intentionally explicit:
\itemize{
\item if \code{thresholds = NULL}, common equally spaced thresholds are generated
from \code{step_span}
\item if \code{thresholds} is a numeric vector, it is used as one common threshold set
\item if \code{thresholds} is a data frame, threshold values may vary by \code{StepFacet}
(currently \code{Criterion} or \code{Rater})
}

Assignment handling is also explicit:
\itemize{
\item \code{"crossed"} uses the full person x rater x criterion design
\item \code{"rotating"} assigns a deterministic rotating subset of raters per person
\item \code{"resampled"} reuses empirical person-level rater profiles stored in
\code{sim_spec$assignment_profiles}, optionally carrying over person-level
\code{Group}
\item \code{"skeleton"} reuses an observed person-by-rater-by-criterion response
skeleton stored in \code{sim_spec$design_skeleton}, optionally carrying over
\code{Group} and \code{Weight}
}

For more controlled workflows, build a reusable simulation specification
first via \code{\link[=build_mfrm_sim_spec]{build_mfrm_sim_spec()}} or derive one from an observed fit with
\code{\link[=extract_mfrm_sim_spec]{extract_mfrm_sim_spec()}}, then pass it through \code{sim_spec}.

Returned data include attributes:
\itemize{
\item \code{mfrm_truth}: simulated true parameters (for parameter-recovery checks)
\item \code{mfrm_truth$signals}: injected DIF and interaction signal tables
\item \code{mfrm_simulation_spec}: generation settings (for reproducibility)
}
}
\section{Interpreting output}{

\itemize{
\item Higher \code{theta} values in \code{mfrm_truth$person} indicate higher person measures.
\item Higher values in \code{mfrm_truth$facets$Rater} indicate more severe raters.
\item Higher values in \code{mfrm_truth$facets$Criterion} indicate more difficult criteria.
\item \code{mfrm_truth$signals$dif_effects} and \code{mfrm_truth$signals$interaction_effects}
record any injected detection targets.
}
}

\section{Typical workflow}{

\enumerate{
\item Generate one design with \code{simulate_mfrm_data()}.
\item Fit with \code{\link[=fit_mfrm]{fit_mfrm()}} and diagnose with \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}.
\item For repeated design studies, use \code{\link[=evaluate_mfrm_design]{evaluate_mfrm_design()}}.
}
}

\examples{
sim <- simulate_mfrm_data(
  n_person = 40,
  n_rater = 4,
  n_criterion = 4,
  raters_per_person = 2,
  seed = 123
)
head(sim)
names(attr(sim, "mfrm_truth"))
}
\seealso{
\code{\link[=evaluate_mfrm_design]{evaluate_mfrm_design()}}, \code{\link[=fit_mfrm]{fit_mfrm()}}, \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}
}
