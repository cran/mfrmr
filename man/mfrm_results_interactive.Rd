% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-results.R
\name{mfrm_results_interactive}
\alias{mfrm_results_interactive}
\title{Interactively choose data-frame columns before calling \code{mfrm_results()}}
\usage{
mfrm_results_interactive(
  data,
  include = "standard",
  output = c("object", "summary", "tables", "html")
)
}
\arguments{
\item{data}{A long-format data frame.}

\item{include}{Passed to \code{\link[=mfrm_results]{mfrm_results()}}.}

\item{output}{Passed to \code{\link[=mfrm_results]{mfrm_results()}}.}
}
\value{
The selected \code{\link[=mfrm_results]{mfrm_results()}} output.
}
\description{
Interactively choose data-frame columns before calling \code{mfrm_results()}
}
\details{
This helper is deliberately opt-in and stops in non-interactive sessions.
It asks the user to choose the person, score, optional weight, and facet
columns, prints reproducible code for the selected roles, then fits the
default legacy-compatible \code{RSM}/\code{JML} route before calling
\code{\link[=mfrm_results]{mfrm_results()}}. Use explicit \code{\link[=fit_mfrm]{fit_mfrm()}} calls in scripts, Quarto
documents, tests, and reproducible analyses.
}
\section{Why this helper is opt-in}{

Interactive prompts are useful at the console but are unsafe defaults for
reproducible analysis, package checks, batch scripts, and manuscripts. The
helper therefore prints replay code and leaves the scripted route explicit.
}

\examples{
if (interactive()) {
  toy <- load_mfrmr_data("example_core")
  res <- mfrm_results_interactive(toy)
}
}
\seealso{
\code{\link[=mfrm_results]{mfrm_results()}}, \code{\link[=fit_mfrm]{fit_mfrm()}}, \code{\link[=run_mfrm_facets]{run_mfrm_facets()}}
}
