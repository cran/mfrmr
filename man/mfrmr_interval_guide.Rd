% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/help_visual_diagnostics.R
\name{mfrmr_interval_guide}
\alias{mfrmr_interval_guide}
\title{Confidence-interval and uncertainty route guide}
\usage{
mfrmr_interval_guide(
  scope = c("all", "visual", "table", "reporting", "fit", "bias", "linking", "gpcm",
    "equivalence", "hierarchical", "shrinkage")
)
}
\arguments{
\item{scope}{Which rows to return: \code{"all"} (default), \code{"visual"},
\code{"table"}, \code{"reporting"}, \code{"fit"}, \code{"bias"}, \code{"linking"}, \code{"gpcm"},
\code{"equivalence"}, \code{"hierarchical"}, or \code{"shrinkage"}.}
}
\value{
A data.frame with columns:
\itemize{
\item \code{Route}
\item \code{Scope}
\item \code{PrimaryHelper}
\item \code{DisplayRoute}
\item \code{DefaultLevel}
\item \code{IntervalColumns}
\item \code{Basis}
\item \code{UseFor}
\item \code{InterpretationBoundary}
\item \code{GPCMStatus}
\item \code{Notes}
}
}
\description{
Return a compact map of the public \code{mfrmr} routes that can expose
confidence intervals or interval-like uncertainty displays. Use this when
you need to know which helper accepts \code{show_ci} or \code{ci_level}, which columns
to look for in \code{draw = FALSE} output, and how strongly the resulting
interval should be interpreted.
}
\details{
The guide is deliberately conservative. It is a namespace and interpretation
map, not a fitted result and not proof that a given interval is available
for a particular run. For run-specific availability, call the listed helper
with \code{draw = FALSE} or inspect the relevant result table.

Most rows use \code{ci_level = 0.95} by default. Some intervals are model-based
Wald intervals, some are delta-method intervals, some are profile or
profile-like intervals when available, and some are plotting overlays around
already-estimated quantities. The \code{Basis} and \code{InterpretationBoundary}
columns are the important guardrails.
}
\examples{
mfrmr_interval_guide()
mfrmr_interval_guide("visual")[, c("Route", "DisplayRoute", "Basis")]
mfrmr_interval_guide("gpcm")[, c("Route", "GPCMStatus", "InterpretationBoundary")]
}
\seealso{
\link{mfrmr_visual_diagnostics}, \code{\link[=visual_reporting_template]{visual_reporting_template()}},
\code{\link[=plot_fair_average]{plot_fair_average()}}, \code{\link[=plot_bias_interaction]{plot_bias_interaction()}},
\code{\link[=plot_displacement]{plot_displacement()}}, \code{\link[=plot_wright_unified]{plot_wright_unified()}},
\code{\link[=plot_rater_severity_profile]{plot_rater_severity_profile()}}, \code{\link[=plot_apa_figure_one]{plot_apa_figure_one()}},
\code{\link[=fit_measures_table]{fit_measures_table()}}
}
\concept{confidence intervals}
\concept{reporting workflow}
\concept{uncertainty displays}
\concept{visual diagnostics}
